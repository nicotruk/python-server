web:
  build: .
  command: gunicorn -w 4 -b 0.0.0.0:5000 app:app --log-file=-
  environment:
    - MONGO_URL=mongodb://db
    - MONGO_PORT=27017
  ports:
    - "5000:5000"
  volumes:
    - .:/python-server
  links:
    - db
db:
  image: mongo:3.0.2